<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>uy.edu.eventos</groupId>
    <artifactId>eventos-uy</artifactId>
    <version>1.0-SNAPSHOT</version>
    <name>EventosUY</name>
    <description>Plataforma eventos.uy - Programacion de Aplicaciones</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
    </properties>

    <dependencies>
        <!--
            forms_rt: soporte en tiempo de ejecucion para los paneles creados con el
            Swing UI Designer de IntelliJ (com.intellij.uiDesigner.core.*).
        -->
        <dependency>
            <groupId>com.intellij</groupId>
            <artifactId>forms_rt</artifactId>
            <version>7.0.3</version>
        </dependency>

        <!-- JPA 3.1 API -->
        <dependency>
            <groupId>jakarta.persistence</groupId>
            <artifactId>jakarta.persistence-api</artifactId>
            <version>3.1.0</version>
        </dependency>

        <!-- Hibernate 6 (implementacion JPA) -->
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-core</artifactId>
            <version>6.6.15.Final</version>
        </dependency>

        <!-- Driver JDBC de PostgreSQL -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.13</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.0</version>
            </plugin>

            <!--
                maven-jar-plugin: escribe la clase de arranque en el MANIFEST del .jar,
                para que "java -jar target/eventos-uy-1.0-SNAPSHOT.jar" funcione.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>presentacion.VentanaPrincipal</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <!--
                exec-maven-plugin: permite correr la aplicacion con "./mvnw exec:java"
                (o ".\mvnw.cmd exec:java" en Windows) sin abrir IntelliJ.
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.1</version>
                <configuration>
                    <mainClass>presentacion.VentanaPrincipal</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>